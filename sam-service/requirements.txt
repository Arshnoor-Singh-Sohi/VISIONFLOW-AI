# =============================================================================
# VisionFlow AI - SAM Service Requirements
# =============================================================================
# Core ML and image processing dependencies for the SAM service

# FastAPI and web server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Image processing
Pillow==10.1.0
opencv-python==4.8.1.78
numpy==1.24.3

# PyTorch (installed via base Docker image, but pinning version)
torch==2.1.0
torchvision==0.16.0

# SAM dependencies (will be installed from git in Dockerfile)
# segment-anything  # Installed from GitHub

# System monitoring
psutil==5.9.6

# HTTP client for health checks
aiohttp==3.9.1
aiofiles==23.2.0

# Data validation and serialization
pydantic==2.5.0

# Logging and monitoring
structlog==23.2.0